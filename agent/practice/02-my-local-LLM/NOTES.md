# 本地部署大语言模型 - 学习笔记

> Hello-Agents 第三章实践后的认知与理解

---

## 实践过程回顾

### 做了什么

按照教程和指导，执行了以下操作：

```bash
# 1. 安装依赖
pip install transformers torch -i https://pypi.tuna.tsinghua.edu.cn/simple

# 2. 设置镜像（加速模型下载）
export HF_ENDPOINT="https://hf-mirror.com"

# 3. 运行脚本
python run_local_llm.py
```

运行后，程序自动下载了一个叫 `Qwen/Qwen1.5-0.5B-Chat` 的模型，然后可以和它对话。

### 我看到的输出

```
使用设备: cpu
正在加载分词器: Qwen/Qwen1.5-0.5B-Chat
正在加载模型: Qwen/Qwen1.5-0.5B-Chat
模型和分词器加载完成！

你: 你好，请介绍一下你自己
AI: 我是来自阿里云的大规模语言模型，我叫通义千问...
```

---

## 初步认知

### 关于模型下载

执行了安装命令，程序自动下载了一些东西。通过 QA 文档，了解到：

- **PyTorch**：一个深度学习框架，用于运行模型
- **Transformers**：Hugging Face 提供的库，用于加载和使用模型
- **模型文件**：从 Hugging Face Hub 下载，存储在本地的缓存目录

目前对这些概念有了初步的认识，知道它们是运行本地模型需要的组件，但具体的技术原理还需要进一步学习。

### 关于模型大小

教程选择了 `Qwen1.5-0.5B-Chat` 这个模型。了解到：

- `0.5B` 表示约 5 亿个参数
- 参数越多，模型越大，需要的内存越多
- 小模型（如 0.5B）可以在 CPU 上运行

初步认识：选择模型时需要考虑硬件资源，不是所有模型都能在本地运行。

### 关于分词器

运行时看到了 `<|im_start|>system` 这样的特殊标记。了解到：

- 分词器负责把文字转换成数字（Token IDs）
- 不同模型有不同的对话格式
- `apply_chat_template` 函数自动处理这些格式

初步认识：模型不直接理解文字，而是理解数字，分词器是文字和数字之间的桥梁。

---

## 实践中的问题

### 下载速度慢

遇到了下载很慢的问题（约 21.5 KB/s）。解决方法是使用国内镜像：

```bash
pip install torch -i https://pypi.tuna.tsinghua.edu.cn/simple
export HF_ENDPOINT="https://hf-mirror.com"
```

> 国内访问国外资源经常遇到速度问题，镜像源是常用的解决方案。

---

## 本地部署 vs 云端 API

通过这次实践，我了解到两种方式的差异：

| 维度 | 云端 API | 本地部署 |
|------|---------|---------|
| 隐私 | 数据上传到服务器 | 数据不出本地 |
| 成本 | 每次调用付费 | 下载后免费使用 |
| 网络依赖 | 需要网络 | 断网也能用 |
| 速度 | 服务器快 | 本地较慢 |

> 本地部署适合处理敏感数据、长期使用的场景。

---

## 模型缓存管理

模型下载后会缓存到本地：

- **默认位置**：`C:\Users\<用户名>\.cache\huggingface\hub`
- **清理方法**：删除缓存目录
- **迁移方法**：设置 `HF_HOME` 环境变量

> 模型文件可能占用大量空间，需要知道如何管理。

---

## 总结

### 我目前的认知状态

| 内容 | 认知程度 |
|------|---------|
| 安装依赖并解决下载问题 | 初步认识 |
| 加载开源模型到本地 | 初步认识 |
| 进行对话交互 | 已能操作 |
| 模型大小与硬件的关系 | 初步认识 |
| 分词器的作用 | 初步认识 |
| 模型缓存管理 | 初步认识 |

### 下一步可以探索

1. 尝试更大的模型（如 Qwen1.5-1.8B-Chat）
2. 调整生成参数观察效果
3. 学习量化技术
4. 了解更多技术原理

---

## 一句话总结

通过这次实践，我对本地部署大语言模型有了初步的认识：安装依赖、下载模型、运行脚本就能在本地和模型对话。但要深入理解技术原理，还需要继续学习。